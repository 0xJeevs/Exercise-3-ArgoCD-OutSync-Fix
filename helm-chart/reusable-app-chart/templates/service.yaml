apiVersion: v1
kind: Service
metadata:
  name: {{ include "reusable-app-chart.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "reusable-app-chart.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
