apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: payment-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/0xJeevs/Exercise-3-ArgoCD-OutSync-Fix.git # Local path or Git URL
    targetRevision: HEAD
    path: payment-service
  destination:
    server: https://kubernetes.default.svc
    namespace: payment-prod
  syncPolicy:
    automated:
      prune: false  # Intentionally disabled for this exercise
      selfHeal: false
